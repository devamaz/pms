header
  nav#nav-parent
    li.nav-item
      a(href="/")
      //- img(src="/assets/police_logo.jpeg" alt="police logo" height="30" width="50")
    li.nav-item
      a(href="/") Home
    li.nav-item
      a(href="/about") About
    li.nav-item
      a(href="/news") News
  span.err= err
  div#access
    - if ( ! succ ) {
      form(class="user-login-reg" method="POST" action="/")

        input(type="text" placeholder="username" name="username" required)
        input(type="password" placeholder="password" name="password" required)

        button(class="login-btn") Login

        a(class="reg-btn" href="/register") Register
        
    - } else {
      li.nav-item
        a(href="/account") Dashboard
    - }